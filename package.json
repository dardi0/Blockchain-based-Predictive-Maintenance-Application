{
  "name": "pdm-blockchain-zk",
  "version": "1.0.0",
  "description": "Predictive Maintenance System with ZK-SNARK Proofs",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat ignition deploy ignition/modules/Deploy.js",
    "deploy:ganache": "hardhat ignition deploy ignition/modules/Deploy.js --network ganache",
    "test": "hardhat test"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.0.0",
    "@nomicfoundation/hardhat-ignition": "^0.15.0",
    "hardhat": "^2.25.0"
  },
  "dependencies": {
    "web3": "^4.0.0"
  }
}
